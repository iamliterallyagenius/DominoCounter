<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.DominoCounter" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/felt</item>
        <item name="colorOnPrimary">@color/ivory</item>
        <item name="colorPrimaryContainer">@color/felt_light</item>
        <item name="colorSecondary">@color/brass</item>
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <!-- Full-bleed camera screen: no surface tint, dark bars. -->
    <style name="Theme.DominoCounter.Camera" parent="Theme.DominoCounter">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>

    <style name="Widget.Domino.ScoreText" parent="TextAppearance.Material3.DisplayLarge">
        <item name="android:includeFontPadding">false</item>
        <item name="android:fontFamily">sans-serif-black</item>
    </style>
</resources>
